@model string

<div class="message-result">
    <button type="button" class="close">&times;</button>
    @if (Model != null && Model != "")
    {
        <p>@Model</p>
    }
    else
    {
        <p>Error inesperado del servidor. Inténtelo nuevamente.</p>
    }
</div>